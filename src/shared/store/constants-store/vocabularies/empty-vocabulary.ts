import {IVocabulary, IVocabularyItem} from "../../../types.ts";

const empty: IVocabularyItem[] = []


export const emptyVocabulary: IVocabulary = {
    id: "",
    name: "Empty vocabulary",
    vocabulary: empty
}