import {IVocabulary} from "../../../types/vocabulary-types.ts";
import {defaultVocabularyData} from "./default-vocabulary-data.ts";

export const DEFAULT_VOCABULARY: IVocabulary = {
    id: "default",
    name: "Default Vocabulary",
    vocabulary: defaultVocabularyData
}